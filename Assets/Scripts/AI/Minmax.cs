using UnityEngine;

public class Minmax : MonoBehaviour
{
    

    int GetMoveCount(int depth)
    {
        if (depth == 0) 
            return 1;

        int numPositions = 0;

        return numPositions;
    }
}
